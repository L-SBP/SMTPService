# Android邮箱项目后端接口和模型设计文档

## 一、项目概述

本文档基于Android邮箱客户端项目，为后端Spring Boot项目提供完整的接口设计和数据模型定义。文档详细描述了所有需要实现的REST API接口、请求/响应模型以及相关的业务逻辑。

**重要说明：** 本项目采用混合通信架构：

- **邮件收发功能**：使用 **SMTP** 和 **POP3** 协议直接与邮件服务器通信，文件上传
- **其他功能**（用户认证、群组管理等）：使用 **HTTP REST API** 与后端服务器通信

这种架构的优势：

1. 邮件协议标准化
2. 其他业务逻辑通过HTTP API处理，便于扩展和管理
3. 客户端本地缓存邮件数据，提升用户体验
4. 降低后端服务器的邮件传输压力

## 二、技术栈

### 后端技术栈

- **框架**: Spring Boot 3.x
- **认证**: Spring Security + JWT
- **数据访问**: Spring Data JPA
- **数据库**: MySQL
- **缓存**: Redis
- **构建工具**: Maven/Gradle
- **API文档**: Swagger/OpenAPI

## 三、通用模型定义

### 3.1 通用响应模型

```kotlin
data class ApiResponse<T>(
    val success: Boolean,      // 操作是否成功
    val data: T?,              // 响应数据
    val message: String?,      // 消息描述
    val errors: List<String>?  // 错误列表
)
```

### 3.2 分页响应模型

```kotlin
data class Page<T>(
    val content: List<T>,      // 当前页数据
    val totalElements: Long,   // 总元素数
    val totalPages: Int,       // 总页数
    val size: Int,             // 每页大小
    val number: Int,           // 当前页码(从0开始)
    val first: Boolean,        // 是否是第一页
    val last: Boolean          // 是否是最后一页
)
```

## 四、用户认证接口 (AuthController)

### 4.1 用户登录

**接口路径**: `POST /api/auth/login`

**请求头**:

```
Content-Type: application/json
```

**请求体**:

```json
{
    "identifier": "string",     // 用户名
    "password": "string"      // 密码
}
```

**响应**:

```json
{
    "success": true,
    "data": {
        "token": "eyJhbGciOiJIUzI1NiJ9...",  // JWT Token
        "user": {
            "id": 1,
            "username": "john_doe",
            "email": "john@example.com",
            "signature": "Best regards, John",
            "isAdmin": false,
            "quotaLimit": 100.0,
            "usedSpace": 15.5,
            "lastLogin": 1640995200000
        }
    },
    "message": "登录成功",
    "errors": null
}
```

**错误响应**:

```json
{
    "success": false,
    "data": null,
    "message": "用户名或密码错误",
    "errors": ["用户名或密码错误"]
}
```

### 4.2 用户注册

**接口路径**: `POST /api/auth/register`

**请求头**:

```
Content-Type: application/json
```

**请求体**:

```json
{
    "username": "string",     // 用户名(唯一)
    "email": "string",        // 邮箱(唯一，格式验证)
    "password": "string"      // 密码(最小6位)
}
```

**响应**:

```json
{
    "success": true,
    "data": {
        "email": "jane@example.com",
        "result": "true"
    },
    "message": "注册成功",
    "errors": null
}
```

### 4.3 用户登出

**接口路径**: `POST /api/auth/logout`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

**响应**:

```json
{
    "success": true,
    "data": "已退出登录",
    "message": "退出成功",
    "errors": null
}
```

## 五、用户管理接口 (UserController)

### 5.1 获取用户资料

**接口路径**: `GET /api/users/profile`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

**响应**:

```json
{
    "success": true,
    "data": {
        "id": 1,
        "username": "john_doe",
        "email": "john@example.com",
        "signature": "Best regards, John",
        "isAdmin": false,
        "quotaLimit": 100.0,
        "usedSpace": 15.5,
        "lastLogin": 1640995200000
    },
    "message": null,
    "errors": null
}
```

### 5.2 更新用户资料

**接口路径**: `PUT /api/users/profile`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
Content-Type: application/json
```

**请求体**:

```json
{
    "id": 1,
    "username": "john_doe_updated",
    "email": "john.new@example.com",
    "signature": "Best regards, John (Updated)",
    "isAdmin": false,
    "quotaLimit": 100.0,
    "usedSpace": 15.5,
    "lastLogin": 1640995200000
}
```

**响应**:

```json
{
    "success": true,
    "data": {
        "id": 1,
        "username": "john_doe_updated",
        "email": "john.new@example.com",
        "signature": "Best regards, John (Updated)",
        "isAdmin": false,
        "quotaLimit": 100.0,
        "usedSpace": 15.5,
        "lastLogin": 1640995200000
    },
    "message": "用户资料更新成功",
    "errors": null
}
```

### 5.3 修改密码

**接口路径**: `PUT /api/users/password`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
Content-Type: application/json
```

**请求体**:

```json
{
    "oldPassword": "old_password",
    "newPassword": "new_password123",
    "confirmPassword": "new_password123"
}
```

**响应**:

```json
{
    "success": true,
    "data": "密码修改成功",
    "message": "密码修改成功",
    "errors": null
}
```

**错误响应**:

```json
{
    "success": false,
    "data": null,
    "message": "原密码不正确",
    "errors": ["原密码不正确"]
}
```

## 六、邮件管理接口 (EmailController)

### 6.1 获取草稿箱邮件列表

**接口路径**: `GET /api/emails/drafts`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

**查询参数**:

- `page`: 页码(从0开始，默认0)
- `size`: 每页大小(默认20)

**响应**:

```json
{
    "success": true,
    "data": {
        "content": [
            {
                "id": 1,
                "sender": "john@example.com",
                "recipients": ["jane@example.com"],
                "cc": [],
                "bcc": [],
                "subject": "草稿邮件示例",
                "body": "这是一封草稿邮件的内容",
                "hasAttachment": false,
                "isRead": false,
                "isStarred": false,
                "size": 1024,
                "receivedTime": 1640995200000,
                "folderType": "DRAFT"
            }
        ],
        "totalElements": 10,
        "totalPages": 1,
        "size": 20,
        "number": 0,
        "first": true,
        "last": true
    },
    "message": null,
    "errors": null
}
```

### 6.2 创建草稿

**接口路径**: `POST /api/emails/drafts`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
Content-Type: application/json
```

**请求体**:

```json
{
    "to": ["jane@example.com"],
    "cc": ["bob@example.com"],
    "bcc": [],
    "subject": "新邮件草稿",
    "body": "这是邮件草稿的内容",
    "attachments": [
        {
            "id": 1,
            "fileName": "document.pdf",
            "fileSize": 102400,
            "contentType": "application/pdf"
        }
    ]
}
```

**响应**:

```json
{
    "success": true,
    "data": 1,
    "message": "草稿创建成功",
    "errors": null
}
```

### 6.3 更新草稿

**接口路径**: `PUT /api/emails/drafts/{draftId}`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
Content-Type: application/json
```

**路径参数**:

- `draftId`: 草稿ID

**请求体**:

```json
{
    "to": ["jane@example.com"],
    "cc": ["bob@example.com", "alice@example.com"],
    "bcc": [],
    "subject": "更新后的邮件草稿",
    "body": "这是更新后的邮件草稿内容",
    "attachments": [
        {
            "id": 1,
            "fileName": "document.pdf",
            "fileSize": 102400,
            "contentType": "application/pdf"
        },
        {
            "id": 2,
            "fileName": "image.jpg",
            "fileSize": 204800,
            "contentType": "image/jpeg"
        }
    ]
}
```

**响应**:

```json
{
    "success": true,
    "data": "草稿更新成功",
    "message": "草稿更新成功",
    "errors": null
}
```

### 6.4 删除草稿

**接口路径**: `DELETE /api/emails/drafts/{draftId}`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

**路径参数**:

- `draftId`: 草稿ID

**响应**:

```json
{
    "success": true,
    "data": "草稿删除成功",
    "message": "草稿删除成功",
    "errors": null
}
```

### 6.5 标记邮件为已读/未读

**接口路径**: `PUT /api/emails/{id}/read`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
Content-Type: application/json
```

**路径参数**:

- `id`: 邮件ID

**请求体**:

```json
true  // 或 false
```

**响应**:

```json
{
    "success": true,
    "data": "标记已读成功",
    "message": "标记已读成功",
    "errors": null
}
```

### 6.6 标记邮件为星标/取消星标

**接口路径**: `PUT /api/emails/{id}/star`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
Content-Type: application/json
```

**路径参数**:

- `id`: 邮件ID

**请求体**:

```json
true  // 或 false
```

**响应**:

```json
{
    "success": true,
    "data": "标记星标成功",
    "message": "标记星标成功",
    "errors": null
}
```

## 八、群组管理接口 (GroupController)

### 8.1 创建群组

**接口路径**: `POST /api/groups`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
Content-Type: application/json
```

**请求体**:

```json
{
    "name": "群组名称",
    "description": "群组描述"
}
```

**响应**:

```json
{
    "success": true,
    "data": {
        "id": 1,
        "name": "开发团队",
        "description": "Android开发团队群组",
        "memberCount": 1,
        "createdBy": 1,
        "createdDate": 1640995200000,
        "lastModified": 1640995200000
    },
    "message": "群组创建成功",
    "errors": null
}
```

### 8.2 更新群组

**接口路径**: `PUT /api/groups`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
Content-Type: application/json
```

**请求体**:

```json
{
    "id": 1,
    "name": "开发团队(更新)",
    "description": "Android开发团队群组(更新)"
}
```

**响应**:

```json
{
    "success": true,
    "data": {
        "id": 1,
        "name": "开发团队(更新)",
        "description": "Android开发团队群组(更新)",
        "memberCount": 1,
        "createdBy": 1,
        "createdDate": 1640995200000,
        "lastModified": 1640995300000
    },
    "message": "群组更新成功",
    "errors": null
}
```

### 8.3 删除群组

**接口路径**: `DELETE /api/groups/{groupId}`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

**路径参数**:

- `groupId`: 群组ID

**响应**:

```json
{
    "success": true,
    "data": "群组删除成功",
    "message": "群组删除成功",
    "errors": null
}
```

### 8.4 获取群组详情

**接口路径**: `GET /api/groups/{groupId}`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

**路径参数**:

- `groupId`: 群组ID

**响应**:

```json
{
    "success": true,
    "data": {
        "id": 1,
        "name": "开发团队",
        "description": "Android开发团队群组",
        "memberCount": 5,
        "createdBy": 1,
        "createdDate": 1640995200000,
        "lastModified": 1640995200000
    },
    "message": null,
    "errors": null
}
```

### 8.5 获取用户创建的群组列表

**接口路径**: `GET /api/groups/created`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

**查询参数**:

- `page`: 页码(从0开始，默认0)
- `size`: 每页大小(默认20)

**响应**:

```json
{
    "success": true,
    "data": {
        "content": [
            {
                "id": 1,
                "name": "开发团队",
                "description": "Android开发团队群组",
                "memberCount": 5,
                "createdBy": 1,
                "createdDate": 1640995200000,
                "lastModified": 1640995200000
            }
        ],
        "totalElements": 10,
        "totalPages": 1,
        "size": 20,
        "number": 0,
        "first": true,
        "last": true
    },
    "message": null,
    "errors": null
}
```

### 8.6 获取用户加入的群组列表

**接口路径**: `GET /api/groups/joined`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

**查询参数**:

- `page`: 页码(从0开始，默认0)
- `size`: 每页大小(默认20)

**响应**: 同创建的群组列表

### 8.7 获取所有群组列表

**接口路径**: `GET /api/groups/all`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

**查询参数**:

- `page`: 页码(从0开始，默认0)
- `size`: 每页大小(默认20)

**响应**: 同创建的群组列表

### 8.8 搜索群组

**接口路径**: `GET /api/groups/search`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

**查询参数**:

- `query`: 搜索关键字
- `page`: 页码(从0开始，默认0)
- `size`: 每页大小(默认20)

**响应**: 同创建的群组列表

### 8.9 添加群组成员

**接口路径**: `POST /api/groups/members`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
Content-Type: application/json
```

**请求体**:

```json
{
    "groupId": 1,
    "accountId": 2
}
```

**响应**:

```json
{
    "success": true,
    "data": "添加成员成功",
    "message": "添加成员成功",
    "errors": null
}
```

### 8.10 移除群组成员

**接口路径**: `DELETE /api/groups/{groupId}/members/{accountId}`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

**路径参数**:

- `groupId`: 群组ID
- `accountId`: 用户账户ID

**响应**:

```json
{
    "success": true,
    "data": "移除成员成功",
    "message": "移除成员成功",
    "errors": null
}
```

### 8.11 获取群组成员列表

**接口路径**: `GET /api/groups/{groupId}/members`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

**路径参数**:

- `groupId`: 群组ID

**查询参数**:

- `page`: 页码(从0开始，默认0)
- `size`: 每页大小(默认20)

**响应**:

```json
{
    "success": true,
    "data": {
        "content": [
            {
                "groupId": 1,
                "accountId": 1,
                "joinedAt": 1640995200000
            },
            {
                "groupId": 1,
                "accountId": 2,
                "joinedAt": 1640995300000
            }
        ],
        "totalElements": 5,
        "totalPages": 1,
        "size": 20,
        "number": 0,
        "first": true,
        "last": true
    },
    "message": null,
    "errors": null
}
```

### 8.12 获取群组成员数量

**接口路径**: `GET /api/groups/{groupId}/member-count`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

**路径参数**:

- `groupId`: 群组ID

**响应**:

```json
{
    "success": true,
    "data": 5,
    "message": null,
    "errors": null
}
```

### 8.13 检查用户是否为群组成员

**接口路径**: `GET /api/groups/{groupId}/members/{accountId}/check`

**请求头**:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

**路径参数**:

- `groupId`: 群组ID
- `accountId`: 用户账户ID

**响应**:

```json
{
    "success": true,
    "data": true,
    "message": null,
    "errors": null
}
```